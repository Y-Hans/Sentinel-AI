# URL-ML Independent Verification

The campaign was executed twice from a clean process with seed 42. The second execution independently retrained all three challengers, regenerated the split, and reproduced the same champion, threshold, confusion matrices, hard-source holdout metrics, and resource benchmark within the recorded floating-point precision. Both executions append to `EXPERIMENT_REGISTRY.jsonl`; historical artifacts remain preserved.

