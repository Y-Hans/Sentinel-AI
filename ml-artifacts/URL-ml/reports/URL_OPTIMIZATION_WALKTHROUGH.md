# URL-ML Optimization Walkthrough

Original model → baseline reproduction → duplicate/domain leakage audit → targeted feature/architecture comparison → validation-only threshold selection → hard-negative/source holdout → separate adversarial transformations → resource benchmark → independent reproducibility requirements → release decision.

Historical artifacts were preserved; experiment records are append-only in `EXPERIMENT_REGISTRY.jsonl`.
